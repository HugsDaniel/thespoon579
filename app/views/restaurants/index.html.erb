<h1>Bienvenue sur la Cuillère !</h1>

<% @restaurants.each do |restaurant| %>
  <!-- <div class="restaurant"> -->
  <%= link_to restaurant_path(restaurant), class: "card" do %>
    <h3>
      <%= restaurant.name %>
    </h3>
    <div>
      <p><strong><%= restaurant.address %></strong></p>
      <p><%= restaurant.rating %> / 5</p>
    </div>
  <% end %>

    <%#= link_to "Voir les détails", restaurant_path(restaurant) %>
  <!-- </div> -->
<% end %>
